# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# local env files
.env*.local

# runtime data
/data/*.sqlite
/data/*.sqlite-journal
/data/*.sqlite-wal
/data/*.sqlite-shm
/data/pdf-sources/*
!/data/pdf-sources/.gitkeep
/public/uploads/*
!/public/uploads/.gitkeep

# misc
.DS_Store
*.tsbuildinfo
npm-debug.log*

# auto-regenerated by `next dev`/`next build` on newer Next versions (generic
# AI-agent-tooling boilerplate, not authored project content - see the
# comment Next itself writes into these files)
/AGENTS.md
/CLAUDE.md
